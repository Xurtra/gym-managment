# Website Renderer App

Public website rendering workspace.

The current slice defines the base public route table and layout model for home, schedule, plans, and signup pages, framework-neutral Website Builder state for the public home, schedule, and plans pages, and public signup/checkout state for online plan selection, form validation, and checkout summary flow.
