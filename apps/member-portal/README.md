# Member Portal App

Member-facing workspace.

The current slice defines the base member portal route table and layout model. Visual implementation can build on these route contracts without changing the auth and navigation behavior.
